# Руководство по управлению Celery в Production

## 🚀 Быстрый старт

### 1. Запуск Celery
```bash
# Запустить воркер
./celery_production.sh start

# Проверить статус
./celery_production.sh status

# Посмотреть логи
./celery_production.sh logs
```

### 2. Перезапуск Celery
```bash
# Перезапустить воркер
./celery_production.sh restart

# Или остановить и запустить отдельно
./celery_production.sh stop
./celery_production.sh start
```

## 📁 Файлы системы

### Основные скрипты:
- `celery_production.sh` - Главный скрипт управления Celery
- `production.env` - Переменные окружения для production
- `setup_celery_autostart.sh` - Настройка автозапуска

### Системные файлы:
- `/var/log/celery/worker.log` - Логи Celery воркера
- `/var/run/celery_worker.pid` - PID файл воркера

## ⚙️ Настройка автозапуска

### Вариант 1: Через cron (рекомендуется)
```bash
# Настроить автозапуск
./setup_celery_autostart.sh setup

# Проверить статус
./setup_celery_autostart.sh status

# Удалить автозапуск
./setup_celery_autostart.sh remove
```

### Вариант 2: Через systemd (для опытных пользователей)
```bash
# Создать systemd сервис (требует root)
sudo ./setup_celery_autostart.sh systemd

# Управление через systemctl
sudo systemctl start vashsender-celery
sudo systemctl status vashsender-celery
```

## 🔧 Конфигурация

### Переменные окружения (production.env):
```bash
# PostgreSQL
POSTGRES_DB=vashsender
POSTGRES_USER=vashsender
POSTGRES_PASSWORD=34t89O7i@
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432

# Redis
REDIS_URL=redis://localhost:6379/0

# Django
DJANGO_SETTINGS_MODULE=core.settings.production
```

### Настройки Celery:
- **Очереди**: campaigns, email, default
- **Concurrency**: 4 воркера
- **Логи**: /var/log/celery/worker.log
- **PID**: /var/run/celery_worker.pid

## 🔍 Диагностика

### Проверка статуса:
```bash
# Подробный статус
./celery_production.sh status

# Тест настроек
./celery_production.sh test

# Просмотр логов в реальном времени
./celery_production.sh logs
```

### Проверка процессов:
```bash
# Все Celery процессы
ps aux | grep celery

# PID воркера
cat /var/run/celery_worker.pid

# Логи системы
journalctl -f | grep celery
```

## 🚨 Решение проблем

### Проблема: Воркер не запускается
1. Проверьте логи: `./celery_production.sh logs`
2. Проверьте настройки: `./celery_production.sh test`
3. Проверьте PostgreSQL: `pg_isready -h 127.0.0.1 -p 5432 -U vashsender`
4. Проверьте Redis: `redis-cli -u redis://localhost:6379/0 ping`

### Проблема: Воркер останавливается после выхода из SSH
1. Убедитесь что используете `./celery_production.sh start`
2. Настройте автозапуск: `./setup_celery_autostart.sh setup`
3. Проверьте что процесс запущен как демон: `ps aux | grep celery`

### Проблема: Рассылки не отправляются
1. Проверьте статус воркера: `./celery_production.sh status`
2. Проверьте логи: `tail -f /var/log/celery/worker.log`
3. Проверьте подключение к PostgreSQL в логах
4. Перезапустите воркер: `./celery_production.sh restart`

### Проблема: Ошибки подключения к базе данных
1. Проверьте переменные окружения в `production.env`
2. Убедитесь что PostgreSQL запущен: `systemctl status postgresql`
3. Проверьте права доступа к базе данных
4. Перезапустите воркер: `./celery_production.sh restart`

## 📊 Мониторинг

### Автоматический мониторинг:
- Cron проверяет статус каждые 5 минут
- Автоматически перезапускает при сбое
- Логирует все действия

### Ручной мониторинг:
```bash
# Статус каждые 30 секунд
watch -n 30 './celery_production.sh status'

# Логи в реальном времени
tail -f /var/log/celery/worker.log

# Системные ресурсы
htop | grep celery
```

## 🔄 Обновление

### При обновлении кода:
```bash
# Перезапустить воркер
./celery_production.sh restart

# Проверить статус
./celery_production.sh status
```

### При изменении настроек:
```bash
# Отредактировать production.env
nano production.env

# Перезапустить воркер
./celery_production.sh restart
```

## 📝 Логи

### Основные файлы логов:
- `/var/log/celery/worker.log` - Логи Celery воркера
- `/var/log/vashsender/django.log` - Логи Django (если настроены)
- `/var/log/vashsender/celery.log` - Логи Celery (если настроены)

### Просмотр логов:
```bash
# Последние 100 строк
tail -n 100 /var/log/celery/worker.log

# В реальном времени
tail -f /var/log/celery/worker.log

# Поиск ошибок
grep -i error /var/log/celery/worker.log
```

---

## 💡 Рекомендации

1. **Всегда используйте** `./celery_production.sh` вместо прямых команд celery
2. **Настройте автозапуск** через `./setup_celery_autostart.sh setup`
3. **Регулярно проверяйте логи** на наличие ошибок
4. **Мониторьте использование ресурсов** системы
5. **Делайте бэкапы** конфигурационных файлов

## 🆘 Экстренные команды

```bash
# Экстренная остановка всех Celery процессов
pkill -f celery

# Принудительная очистка
rm -f /var/run/celery_worker.pid
pkill -9 -f celery

# Экстренный перезапуск
./celery_production.sh stop
sleep 5
./celery_production.sh start
```
